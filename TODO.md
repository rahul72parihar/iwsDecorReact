# TODO

## Header sidebar / mobile drawer
- [ ] Update `CategoryBar.jsx` to implement a real off-canvas sidebar drawer + overlay.
- [ ] Add `onClose` prop support and close the drawer when a nav link is clicked.
- [ ] Update `CategoryBar.css` with drawer/overlay/close button styles + responsive behavior.
- [ ] Ensure `Header.jsx` passes `onClose` correctly (already present) and drawer toggles on hamburger.
- [ ] Run dev server and verify: hamburger opens drawer, overlay click closes, clicking link closes.

