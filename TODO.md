- [x] Add global Header and Footer to `src/pages/User/Profile.jsx` (including loading and logged-out states).
- [ ] Adjust `ProfilePage.css` only if needed to prevent overlap/spacing issues with Footer.
- [x] Verify by running/reloading app and navigating to `/account/profile` (build succeeded).


